<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>ログイン</title>
    <link rel="stylesheet" href="/core/admin/css/login.css">
</head>
<body>
    <header>
        <h1>dango</h1>
    </header>
    <article>
        <div id="container">
            <div id="main">
                <form name="login_form" action="/admin/login" method="post" enctype="application/x-www-form-urlencoded">
                    <fieldset>
                        <legend>ログイン</legend>
                        <label for="login_id">ログインID</label>
                        <input type="text" name="login_id" id="login_id" value="{{ login_id }}">
                        {% if errors is not null %}
                        {% if errors.login_id is defined %}
                        <div>{{ errors.login_id }}</div>
                        {% endif %}
                        {% endif %}
                        <label for="password">パスワード</label>
                        <input type="text" name="password" id="password" value="{{ password }}">
                        {% if errors is not null %}
                        {% if errors.password is defined %}
                        <div>{{ errors.password }}</div>
                        {% endif %}
                        {% endif %}
                    </fieldset>
                    {% if errors.screen_message is defined %}
                    <div>{{ errors.screen_message }}</div>
                    {% endif %}
                    <input type="submit" name="submit" value="ログイン">
                </form>
            </div>
        </div>
    </article>
    <footer>
        <small>Copyright &copy; 2020 2bo0. All Rights Reserved.</small>
    </footer>
</body>
</html>